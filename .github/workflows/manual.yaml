name: Manual
on:
  workflow_dispatch:
    input:
      greeting:
        type: string
        default: Hello
        required: true
        description: A cheerful world
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ inputs.greeting }}"